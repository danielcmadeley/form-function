# Glossary

- **Engine**: Python runtime workspace in `engine/`.
- **Platform**: TypeScript Turborepo workspace in `platform/`.
- **Contract**: Any explicit interface shared across workspaces (API, schema, protocol).
- **Decision Log**: Historical record of non-trivial tradeoffs in `plans/*/decisions.md`.
