# API Plan

## Scope
`platform/apps/api` (edge API surface and contract boundary with engine/platform clients).

## Purpose
- Track endpoint design, auth, and runtime concerns.
- Record contract changes and compatibility strategy with engine + web clients.

## Next Updates
- Define initial endpoint inventory and versioning policy.
- Document Cloudflare runtime bindings and environment model.
